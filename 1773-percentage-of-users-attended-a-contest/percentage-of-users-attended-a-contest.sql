# Write your MySQL query statement below
select Register.contest_id,ROUND(COUNT(Register.contest_id) * 100.0/(SELECT COUNT(*) FROM Users),2) as percentage from Register join Users on Register.user_id=Users.user_id group by Register.contest_id order by percentage desc,contest_id asc;